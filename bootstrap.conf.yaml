- defaults:
        link:
              relink: true
              force: true
              create: true
- clean: ["~"]

# Bitwarden via Brew (not we disable brew install because that does not work well)
- cask:
        - bitwarden
